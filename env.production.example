# =============================================================================
# Rule MCP Server - Production Environment Configuration Example
# =============================================================================
# 
# 使用方法:
# 1. このファイルをコピーして .env.production にリネーム
# 2. 以下の値を実際の本番環境に合わせて設定
# 3. 本番環境では必ず強力な秘密鍵を生成してください
#
# 秘密鍵の生成方法:
# - JWT_SECRET: openssl rand -hex 32
# - または: python3 -c "import secrets; print(secrets.token_hex(32))"
# - または: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL データベース設定
DB_HOST=localhost
DB_PORT=5432
DB_USER=rulemcp
DB_PASSWORD=your_secure_production_password
DB_NAME=rulemcp

# =============================================================================
# SECURITY CONFIGURATION (本番環境では必須)
# =============================================================================
# JWT秘密鍵 (本番環境では必須)
# 生成方法: openssl rand -hex 32
JWT_SECRET=your-strong-random-secret-here

# CORS許可オリジン (本番環境では必須)
# カンマ区切りで許可するオリジンを指定
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# サーバーポート (デフォルト: 18080)
PORT=18080

# 環境設定 (本番環境では必ず production)
ENV=production

# =============================================================================
# MCP CONFIGURATION
# =============================================================================
# MCP Server URL (for external connections)
MCP_SERVER_URL=https://yourdomain.com:18080

# MCP API Key (optional, for authentication)
MCP_API_KEY=your_production_api_key

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
# Log Level (本番環境では info または warn を推奨)
LOG_LEVEL=info

# =============================================================================
# RATE LIMITING (本番環境では有効化を推奨)
# =============================================================================
# Enable rate limiting (本番環境では true を推奨)
RATE_LIMIT_ENABLED=true

# Rate limit requests per minute
RATE_LIMIT_REQUESTS=100
