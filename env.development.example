# =============================================================================
# Rule MCP Server - 開発環境設定例
# =============================================================================
# 
# 使用方法:
# 1. このファイルをコピーして .env.development にリネーム
# 2. 以下の値を開発環境に合わせて設定
# 3. 開発環境では簡易的な設定でも動作します
#
# =============================================================================

# =============================================================================
# データベース設定
# =============================================================================
# PostgreSQL データベース設定
DB_HOST=localhost
DB_PORT=5432
DB_USER=rulemcp
DB_PASSWORD=rulemcp123
DB_NAME=rulemcp

# =============================================================================
# セキュリティ設定（開発環境用）
# =============================================================================
# JWT秘密鍵 (開発環境では簡易的でも可)
JWT_SECRET=development-secret-key-change-in-production

# CORS許可オリジン (開発環境用)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:13000,http://localhost:18080,http://localhost:18000

# =============================================================================
# サーバー設定
# =============================================================================
# サーバーポート (デフォルト: 18080)
PORT=18080

# 環境設定 (開発環境)
ENV=development

# =============================================================================
# MCP設定
# =============================================================================
# MCPサーバーURL（外部接続用）
MCP_SERVER_URL=http://localhost:18080

# MCP APIキー（オプション、認証用）
MCP_API_KEY=

# =============================================================================
# ログ設定
# =============================================================================
# Log Level (開発環境では debug を推奨)
LOG_LEVEL=debug

# =============================================================================
# RATE LIMITING (開発環境では無効化でも可)
# =============================================================================
# Enable rate limiting (開発環境では false でも可)
RATE_LIMIT_ENABLED=false

# Rate limit requests per minute
RATE_LIMIT_REQUESTS=1000
