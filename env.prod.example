# 本番環境用環境変数設定例
# 使用方法: 
# 1. このファイルを .env.prod にコピー
# 2. 値を本番環境に合わせて変更
# 3. docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d

# データベース設定
DB_NAME=rule_mcp_db_prod
DB_USER=rule_mcp_user_prod
DB_PASSWORD=your_secure_password_here

# JWT設定
JWT_SECRET=your_very_secure_jwt_secret_key_here_change_in_production

# ログレベル
LOG_LEVEL=info

# 環境設定
ENVIRONMENT=production

# ポート設定（Docker内部用）
PORT=8080
